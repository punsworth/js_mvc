<% $.each(this, function(i, activity){ %>
  <li <%= ($el) -> $el.model(activity) %>>
  	<a href="activityDetails.html?activity=<%= activity.id %>" class='activityDetails'>
		<%= activity.title %>
		<br />
		<%= activity.description %>
    </a>
  </li>
<% }) %>